## 1. Layout State and Persistence

- [x] 1.1 Add pure adaptive-layout calculations and deterministic breakpoint, ratio, and target-width tests
- [x] 1.2 Add the validated `overlaySplitRatio` local setting and persistence coverage

## 2. Adaptive Overlay UI

- [x] 2.1 Render transcript and responses as independently scrolling side-by-side panes at wide widths while preserving compact tabs
- [x] 2.2 Add the draggable bounded divider and restore the saved ratio
- [x] 2.3 Auto-expand and recenter once for the first response when the display can accommodate split view

## 3. Verification

- [x] 3.1 Run focused tests, lint, typecheck, renderer build, and strict OpenSpec validation
- [x] 3.2 Package and relaunch Raven for manual wide/narrow overlay verification
